namespace DocumentModel.BaseTypes.Interfaces;

/// <summary>
/// Interface for percent value types, representing a value as a percentage (e.g., 50% as 0.5).
/// This interface can be implemented by types that encapsulate percentage values
/// and provide methods for conversion to and from percentage representations.
/// It serves as a marker interface to identify types that represent percentage values in the DocumentModel base types library.
/// </summary>
public interface IPercentValue
{
  
}