<Window x:Class="ModelGenApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ModelGenApp"
        xmlns:cmd="clr-namespace:ModelGenApp.Commands"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
  <Window.Resources>
    <local:Commander x:Key="Commander"/>
  </Window.Resources>
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="20" />
      <RowDefinition Height="*"/>
      <RowDefinition Height="20" />
    </Grid.RowDefinitions>
    <Menu Grid.Row="0">
      <MenuItem Header="Start" Command="{Binding Source={StaticResource Commander}, Path=Start}"/>
    </Menu>
  </Grid>
</Window>
