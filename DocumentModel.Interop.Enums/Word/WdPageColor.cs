namespace DocumentModel.Interop.Word;

/// <summary>
/// Constants that represent background page color in reading mode, returned by and passed to PageColor.
/// </summary>
public enum WdPageColor
{
  /// <summary>
  /// Constants that represent background page color in reading mode, returned by and passed to PageColor.
  /// </summary>
  wdPageColorNone = unchecked((int)0),
  /// <summary>
  /// Constants that represent background page color in reading mode, returned by and passed to PageColor.
  /// </summary>
  wdPageColorSepia = unchecked((int)1),
  /// <summary>
  /// Constants that represent background page color in reading mode, returned by and passed to PageColor.
  /// </summary>
  wdPageColorInverse = unchecked((int)2)
}
